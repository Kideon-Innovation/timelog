{
  "name": "kideon-time",
  "version": "1.0.0",
  "description": "KIDEON time — local-only Zeiterfassung PWA für Steuerberater/Kanzleien.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "vite": "7.3.5",
    "vite-plugin-pwa": "1.3.0"
  }
}
